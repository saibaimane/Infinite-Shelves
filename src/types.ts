export type LibraryCard = {
  name: string
  creator: string
  description: string
  tags: string[]
  gradient: string
}

export type ShelfOption = {
  title: string
  description: string
  icon: string
}
